# UniV2X TensorRT/INT8 4090 Handoff

Timeline timestamps use the active process timezone (PDT, UTC-07:00).

Status is fail-closed: the cooperative model uses two sequential agent engines, and numerical stability plus full 675-frame metrics are required before a precision is called complete.

## Local lineage

- Working root: `/home/lixingfeng/UniAD_examine/UniV2X`
- Checkpoint: `/home/lixingfeng/UniAD_examine/UniV2X/ckpts/univ2x_coop_e2e_stg2.pth`
- Validation set: 675 cooperative frames, existing read-only dataset links
- Calibration: 152/1521 training samples (about 10%), scene-uniform, collected separately for infrastructure and ego inputs
- Runtime environment: `univ2x_trt_runtime`
- Build/quantization environment: `modelopt`
- Shared ModelOpt editable source target restored at `/home/lixingfeng/UniAD_examine/HEAL/prune_model/Model-Optimizer-0.29.0`; UniV2X also retains its equivalent local 0.29 source tree
- CUDA/NVCC/G++: Conda environment toolchain only
- TensorRT: 10.9.0.34 from `/home/lixingfeng/uniad-trt/TensorRT-10.9_x86_cu118`; its Python binding, plugin dependencies, and engine deserialization were verified without reinstalling or modifying `modelopt`
- Current deployment GPU: RTX 4090, logical GPU 7

## Why there are two engines

The checkpoint represents one cooperative UniV2X system, but inference has two different input contracts and state machines. The infrastructure agent runs first, then selected road-side tracks, map/reference tensors, occupancy features, and transforms are prepared for the ego agent. Exporting one infrastructure engine and one ego engine preserves that sequential contract; they are not duplicate models and are not invoked as a single fused forward.

## Implemented deployment behavior

- Separate infrastructure and ego ONNX export, calibration, profiles, and engines.
- Explicit-QDQ INT8 with global MatMul exclusion.
- Relative per-scene timestamps are formed on the host before float32/FP16 conversion; absolute epoch timestamps around `1.626e9` overflow FP16 and caused the earlier non-finite state.
- Inverse-sigmoid/log chains use finite epsilon/floor rewrites; occupancy `-inf` sentinels use a finite `-10000` invalid value.
- Sensitive `lane_reference`, timestamp/BEV, and MSDA lineages are constrained to FP32 where required by numerical tests.
- Temporal track input is padded to 960 rows with the graph's `-10000` invalid sentinel.
- Cooperative track input is padded to 64 rows with an out-of-range matched index so padding is excluded from fusion.
- One execution context per engine/profile is retained; fixed shapes prevent per-frame Myelin execution-configuration rebuilds.
- Runtime records infrastructure, ego, combined forward, and full end-to-end latency separately.

## Completion matrix

| Item | State | Evidence/notes |
| --- | --- | --- |
| PyTorch FP32 full 675-frame baseline | Completed | Full task and latency summaries retained under `UniV2X/evidence/pytorch_fp32_full675`. |
| FP32 two-engine export/build smoke | Completed | Dynamic-shape timing is superseded by the fixed-shape protocol. |
| FP32 semantic-parity full 675 evaluation | Completed, accepted | Corrected raw-map and explicit map-position graph; all 675 frames completed with zero fallback. Detection/tracking/map/planning match the PyTorch baseline closely; evidence is under `UniV2X/evidence/fp32_semantic_parity_full675`. |
| Legacy fixed-shape FP32/FP16/INT8 evaluations | Superseded | These finite runs used the pre-parity graph and remain only as historical diagnostics under their existing evidence directories. |
| Corrected-graph FP16 build and full evaluation | Completed | Fresh corrected infrastructure/ego engines completed 675/675 frames with finite output and zero fallback. |
| Corrected-graph INT8 calibration/build/full evaluation | Completed | Both agents were calibrated separately with 152 training frames each; fresh stable engines completed 675/675 frames with finite output and zero fallback. |
| CARLA live capture and open-loop engine replay | Completed, diagnostic only | Captured one 20-frame Town03 sequence from a live server and replayed FP32/FP16/INT8 on identical inputs. This is not a route-based closed-loop score. |
| CARLA closed-loop route evaluation | Post-process/gear-corrected custom-route diagnostic completed | PyTorch and corrected FP32/FP16/INT8 use independent intrinsics, occupancy/drivable correction, explicit forward gear, dense route progress, collision groups, and blocked termination. This runner bypasses the official manager/scorer, so no leaderboard driving score is claimed. |

## Accepted FP32 semantic-parity result

The corrected deployment exports data-dependent map candidates and reconstructs map overlap suppression on the host, instead of freezing Python control flow during tracing. It also promotes the static map-position encoding to an explicit cached input, removing the graph-internal Myelin reconfiguration path. Infrastructure and ego retain separate fixed `960/64` contracts and one execution context each.

| Metric | PyTorch FP32 | Corrected TensorRT FP32 | Delta (TRT - PyTorch) |
| --- | ---: | ---: | ---: |
| car-only mAP (4 distance thresholds) | 0.295075 | 0.298185 | +0.003110 |
| car-only AMOTA | 0.236400 | 0.236203 | -0.000197 |
| mAP | 0.051554 | 0.051763 | +0.000210 |
| NDS | 0.051359 | 0.051958 | +0.000599 |
| AMOTA | 0.109535 | 0.112422 | +0.002887 |
| AMOTP | 1.776034 | 1.776355 | +0.000321 |
| map drivable IoU | 0.756636 | 0.759091 | +0.002455 |
| map lanes IoU | 0.177356 | 0.181120 | +0.003764 |
| map crossing IoU | 0.196882 | 0.203026 | +0.006145 |
| map contour IoU | 0.154628 | 0.158483 | +0.003855 |
| occupancy IoU class 0 / 1 | 22.5408 / 25.7962 | 21.8469 / 25.4737 | -0.6939 / -0.3225 pp |
| planning mean L2 over 10 steps | 2.8641 m | 2.8565 m | -0.0076 m |
| planning mean point collision | 0.0741% | 0.0593% | -0.0148 pp |
| planning mean box collision | 0.4889% | 0.3704% | -0.1185 pp |
| forward mean / p50 / p99 | 805.595 / 760.825 / 1290.937 ms | 316.454 / 315.835 / 337.023 ms | 2.55x mean speedup |
| end-to-end mean / p50 / p99 | 2330.759 / 925.171 / 8927.112 ms | 826.203 / 456.331 / 3413.873 ms | 2.82x mean speedup* |

`*` The corrected TensorRT run used eight DataLoader workers, while the retained PyTorch latency baseline predates the worker-default change. Forward speedup is the meaningful backend comparison; the end-to-end ratio is reported but is not a controlled same-worker comparison.

The accepted engines were built with TensorRT `10.9.0.34`, TF32 disabled, and the isolated CUDA 11.8 toolchain. Infrastructure is 559,108,180 bytes, built in 189.833 s, SHA-256 `dd2f2119...645fda`; ego is 622,267,500 bytes, built in 209.650 s, SHA-256 `8defaf06...1c7d`. The full run completed 675/675 frames with infrastructure/ego fallback counts `0/0` and 675 dynamic map reconstructions per agent.

## Corrected-graph full precision comparison

FP16 and INT8 were rebuilt from the accepted raw-map/explicit-position graph. INT8 uses separate infrastructure and ego entropy calibrations, each with 152 scene-uniform training frames (about 10% of the 1521-frame training set); validation frames were not used. MatMul remains excluded from QDQ, sensitive timestamp/map-reference lineages remain FP32 where required, and both agents use one retained context with fixed `960/64` shapes.

This is not Conv-only quantization and is not literal every-operator INT8. The infrastructure graph has 525 Q / 731 DQ nodes and 937 INT8 initializers: INT8 weights cover all 159 Conv and 47 of 49 Gemm nodes, while 0 of 675 MatMul nodes are weight-quantized. The ego graph has 548 Q / 765 DQ nodes and 982 INT8 initializers: all 165 Conv and 52 of 61 Gemm nodes use INT8 weights, while 0 of 712 MatMul nodes do. Activation QDQ boundaries additionally span Add, pooling, resize, normalization, shape, and deformable-convolution-adjacent paths. Such adjacency records the precision boundary but does not by itself prove that TensorRT selected a native INT8 kernel for a custom plugin or shape layer.

| Metric | PyTorch FP32 | TensorRT FP32 | TensorRT FP16 | TensorRT INT8(EQ)+FP16 |
| --- | ---: | ---: | ---: | ---: |
| car-only mAP | 0.295075 | 0.298185 | 0.286687 | 0.279633 |
| car-only AMOTA | 0.236400 | 0.236203 | 0.200524 | 0.180013 |
| mAP / NDS | 0.051554 / 0.051359 | 0.051763 / 0.051958 | 0.049720 / 0.050910 | 0.049027 / 0.049887 |
| AMOTA / AMOTP | 0.109535 / 1.776034 | 0.112422 / 1.776355 | 0.092515 / 1.789994 | 0.087995 / 1.796529 |
| map drivable / lanes IoU | 0.756636 / 0.177356 | 0.759091 / 0.181120 | 0.731524 / 0.149922 | 0.728967 / 0.145431 |
| map crossing / contour IoU | 0.196882 / 0.154628 | 0.203026 / 0.158483 | 0.181886 / 0.124378 | 0.175377 / 0.121112 |
| occupancy IoU class 0 / 1 | 22.5408 / 25.7962 | 21.8469 / 25.4737 | 22.2951 / 25.3255 | 21.8453 / 24.8312 |
| occupancy PQ class 0 / 1 | 2.0404 / 0.0632 | 2.0214 / 0.0725 | 1.9705 / 0.0670 | 1.7235 / 0.0578 |
| planning mean L2 | 2.8641 m | 2.8565 m | 2.8904 m | 2.8292 m |
| planning point / box collision | 0.0741% / 0.4889% | 0.0593% / 0.3704% | 0.0444% / 0.3704% | 0.0296% / 0.3407% |
| engine-only sum mean / p50 / p99 | n/a | 290.120 / 289.388 / 300.288 ms | 228.800 / 228.154 / 239.753 ms | 198.703 / 196.804 / 214.382 ms |
| complete forward mean / p50 / p99 | 805.595 / 760.825 / 1290.937 ms | 316.454 / 315.835 / 337.023 ms | 255.068 / 253.575 / 281.016 ms | 227.410 / 225.842 / 256.523 ms |
| end-to-end mean / p50 / p99 | 2330.759 / 925.171 / 8927.112 ms | 826.203 / 456.331 / 3413.873 ms | 779.290 / 486.431 / 2762.912 ms | 862.901 / 378.026 / 4733.538 ms |

The official UniV2X `AMOTA=0.239` value is a car-only score. It therefore matches the PyTorch `0.236400` and TensorRT FP32 `0.236203` results within normal reproduction tolerance; it must not be compared with the three-class aggregate AMOTA. Divider IoU is undefined for all outputs because the evaluator has no valid divider denominator. The corrected forward trend is `FP32 > FP16 > INT8`: FP16 is `1.24x` faster than TensorRT FP32, INT8 is `1.39x` faster, and INT8 is `1.12x` faster than FP16. End-to-end mean is not monotonic because periodic CPU/DataLoader wait dominates the tail; p50 and the synchronized forward columns are the backend comparison.

FP32 meets the parity gate for detection, tracking, map, and planning; occupancy differs by at most `0.694` percentage points. Reduced precision has a controlled but visible tracking/map decline. INT8 mAP is `5.3%` below the TensorRT FP32 value, AMOTA is `21.7%` below it, and lanes IoU is `19.7%` below it; this is not presented as exact accuracy parity.

## Superseded pre-parity precision results

The table below is retained only to document the old graph and its latency ordering. Its FP16 and INT8 accuracy values must not be compared with the accepted corrected FP32 baseline as quantization loss, because they predate the semantic fixes.

| Metric | PyTorch FP32 | TensorRT FP32 fixed 960/64 | TensorRT FP16 fixed 960/64 | TensorRT INT8(EQ)+FP16 fixed 960/64 |
| --- | ---: | ---: | ---: | ---: |
| mAP | 0.051554 | 0.028844 | 0.028033 | 0.027856 |
| NDS | 0.051359 | 0.038054 | 0.036806 | 0.036338 |
| AMOTA | 0.109535 | 0.079392 | 0.065352 | 0.062914 |
| AMOTP | 1.776034 | 1.848940 | 1.869092 | 1.870615 |
| map drivable IoU | 0.756636 | 0.309360 | 0.308909 | 0.299387 |
| map lanes IoU | 0.177356 | 0 | 0 | 0 |
| map divider IoU | undefined | undefined | undefined | undefined |
| map crossing IoU | 0.196882 | 0 | 0 | 0 |
| map contour IoU | 0.154628 | 0 | 0 | 0 |
| occupancy IoU class 0 / 1 | 22.5408 / 25.7962 | 13.4097 / 10.1413 | 13.5783 / 9.8792 | 13.0702 / 9.2406 |
| occupancy PQ class 0 / 1 | 2.0404 / 0.0632 | 3.7529 / 0.1314 | 3.7668 / 0.1478 | 3.9587 / 0.0696 |
| occupancy SQ class 0 / 1 | 63.2126 / 60.2591 | 63.2883 / 59.4070 | 63.9376 / 58.8232 | 62.8326 / 60.2019 |
| occupancy RQ class 0 / 1 | 3.2278 / 0.1049 | 5.9299 / 0.2212 | 5.8914 / 0.2513 | 6.3004 / 0.1156 |
| planning L2 at step 1 | 1.0857 m | 0.6680 m | 0.6707 m | 0.6397 m |
| planning L2 at step 10 | 5.1047 m | 5.2434 m | 5.2967 m | 5.3092 m |
| planning mean L2 over 10 steps | 2.8641 m | 2.8240 m | 2.8557 m | 2.8091 m |
| planning mean point collision over 10 steps | 0.0741% | 0.0889% | 0.0889% | 0.0444% |
| planning mean box collision over 10 steps | 0.4889% | 0.6222% | 0.5481% | 0.4593% |
| forward mean / p50 / p99 | 805.595 / 760.825 / 1290.937 ms | 315.491 / 314.289 / 341.585 ms | 235.290 / 233.845 / 260.105 ms | 219.240 / 217.340 / 246.817 ms |
| end-to-end mean / p50 / p99 | 2330.759 / 925.171 / 8927.112 ms | 1847.615 / 1267.699 / 5756.992 ms | 2040.711 / 396.823 / 8627.715 ms | 2053.752 / 359.777 / 9162.229 ms |

Divider IoU is explicitly undefined in all four evaluator outputs because this validation result has no valid divider IoU denominator; it is not treated as zero. This evaluator does not emit a separate motion-forecasting headline metric, so no motion score is invented from intermediate tensors.

The same-runtime forward trend is `FP32 315.491 ms > FP16 235.290 ms > INT8 219.240 ms`, matching the expected qualitative precision trend. FP16 is about `1.34x` faster than FP32, INT8 is about `1.44x` faster than FP32, and INT8 is about `1.07x` faster than FP16. End-to-end mean is not monotonic because CPU sample preparation periodically drains the DataLoader prefetch queue; it is not used to infer engine compute scaling.

The former FP32 gap was traced to export/runtime semantics rather than calibration: ONNX tracing froze map-head data-dependent control flow, and the static map-position subgraph triggered configuration rebuilding. Raw map outputs plus host postprocessing restore dynamic semantics, while the explicit cached position input removes the problematic subgraph. The legacy first-frame audit remains at `UniV2X/evidence/fp32_export_parity/frame0_summary.json` as evidence of the pre-fix defect.

## CARLA Town03 open-loop diagnostic

The live CARLA 0.9.10.1 server produced one synchronized two-camera sequence with 20 frames, 20 traffic vehicles, zero capture-time collisions, and zero lane invasions. The same saved inputs were used for all engines; two warmup frames were excluded from latency.

| Precision | Forward mean / p50 / p99 | End-to-end mean / p50 / p99 | Planning mean L2 to FP32 | Finite / fallbacks |
| --- | ---: | ---: | ---: | ---: |
| FP32 | 359.287 / 359.439 / 370.239 ms | 509.015 / 509.539 / 547.036 ms | reference | 20/20, 0/0 |
| FP16 | 267.772 / 266.430 / 288.704 ms | 408.404 / 404.962 / 444.203 ms | 0.1396 m | 20/20, 0/0 |
| INT8(EQ)+FP16 | 247.578 / 246.288 / 255.669 ms | 392.712 / 395.019 / 438.653 ms | 0.3942 m | 20/20, 0/0 |

The latency trend is `FP32 > FP16 > INT8`, but the CARLA detection center-match diagnostic is not comparable to the validation benchmark and is weak on this domain-shifted sample: FP32/FP16 each matched 1 of 29 visible ground-truth centers, while INT8 matched 0. These files are retained under `UniV2X/evidence/carla_open_loop_capture20`.

## CARLA Town03 model-controlled closed-loop diagnostic (historical, superseded)

The previous model-controlled rerun is retained for root-cause history only. The route-conditioned controller below supersedes it: global navigation controls steering while the model trajectory controls speed. Ego and infrastructure cameras retain independent `100.6` and `47.9` degree fields of view.

| Backend | Frames / result | Dense-route progress | Unique collisions / lane invasions | Combined forward mean / p50 / p99 | Service E2E mean / p50 / p99 |
| --- | ---: | ---: | ---: | ---: | ---: |
| PyTorch FP32 | 711 / blocked | 1.7563% | 30 / 6 | 571.992 / 567.975 / 621.317 ms | 722.179 / 721.860 / 774.981 ms |
| TensorRT FP32 | 500 / blocked | 1.6710% | 20 / 4 | 301.013 / 297.069 / 316.064 ms | 450.057 / 453.727 / 503.837 ms |
| TensorRT FP16 | 500 / blocked | 1.6710% | 20 / 4 | 237.689 / 233.631 / 257.349 ms | 389.487 / 390.323 / 433.050 ms |
| TensorRT INT8(EQ)+FP16 | 755 / blocked | 1.7563% | 32 / 6 | 206.776 / 202.058 / 277.676 ms | 366.795 / 360.165 / 447.410 ms |

These rows document the previous single-target controller failure and are not used as the final INT8 CARLA result.

The reused asset tree contains a 42-route catalogue with 4829 scenario trigger-event configurations, plus 233 training-split, 105 evaluation, and 66 additional route definitions. This evidence executed only `routes_town03_1.xml` and spawned zero scenario events. It can report progress, collision groups, lane invasions, blocked reason, speed, and latency, but is not an official V2Xverse Driving Score because it bypasses the route manager and scorer.

## CARLA Town03 route-conditioned INT8 full-route rerun

The corrected run uses the same dense 1171-waypoint, 1181.667 m route, synchronized ego/roadside RGB cameras, no traffic, 0.05 s synchronous ticks, inference every 10 frames, global-route steering, the official V2X first-three-segment speed estimate with EMA `0.35`, and a 500-frame blocked threshold. Infrastructure and ego remain two sequential fixed-shape TensorRT engines, and their latency is reported separately.

| Backend | Frames / result | Progress / final distance | Collision / lane invasion | Infrastructure forward mean / p50 / p99 | Ego forward mean / p50 / p99 | Combined forward mean / p50 / p99 | Service E2E mean / p50 / p99 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| TensorRT INT8(EQ)+FP16 | 3526 / destination reached | 99.5845% / 4.687 m | 0 / 28 | 110.793 / 110.035 / 118.688 ms | 83.608 / 82.934 / 90.360 ms | 200.916 / 199.500 / 216.185 ms | 354.481 / 354.120 / 378.505 ms |

The earlier blocked runs were not caused by INT8 or by the two-engine design. The custom controller steered from a single model point, did not follow the dense GlobalRoutePlanner path, and the original 1200-frame limit represented only 60 s for a route requiring roughly 1181.7 m. The corrected route reaches the destination with zero collision callbacks and zero accepted cooperative-track recoveries. The raw result is in `/data/lxf/univ2x_deployment_outputs/semantic_parity_20260810/carla_route_conditioned_v3_fullroute6500_20260812/tensorrt_int8/closed_loop_metrics.json`, with a source-only summary at `UniV2X/evidence/carla_route_conditioned_int8_fullroute/summary.json`. This custom route protocol is not an official V2Xverse Driving Score.

## 2026-08-16T01:45:24-07:00 (PDT) - Canonical artifact relocation

- The canonical source directory is `/home/lixingfeng/UniAD_examine/DL4AGX/AV-Solutions/univ2x-trt`.
- `univ2x-trt/artifacts` now links directly to `/data/lxf/univ2x_deployment_outputs/semantic_parity_20260810`, which contains the corrected FP32/FP16/INT8 675-frame evaluations, calibration outputs, engines, ONNX external data, and CARLA diagnostic outputs.
- The former 30G generated tree at `/home/lixingfeng/UniAD_examine/UniV2X/deploy_int8_legacy_20260816/artifacts` was removed as superseded generated output. The compatibility directory retains only a small source copy; `/home/lixingfeng/UniAD_examine/UniV2X/deploy_int8` remains a symlink to the canonical source directory.
- No `/data` dataset or checkpoint was changed. Future UniV2X engine/ONNX/calibration outputs must be written under `/data/lxf/univ2x_deployment_outputs` and consumed through the project symlink.

## Data-loader latency finding

After excluding 20 warmup frames, FP16 data wait was p50 `0.235 ms`, mean `1588.289 ms`, and p99 `8138.408 ms`. Of 655 measured frames, 187 exceeded 1 s and 142 exceeded 5 s. Storage sampling showed zero I/O wait and low disk utilization; the four DataLoader workers used for these completed runs were CPU-bound preparing cooperative samples, and the fast engine periodically drained their prefetch queue. Pure forward latency is therefore reported separately from end-to-end latency.

The PyTorch and TensorRT evaluation entry points now default to eight DataLoader workers, matching `workers_per_gpu=8` in the model configuration. The corrected FP32 run used eight workers; the legacy precision table and retained PyTorch baseline used four. A same-host, same-load PyTorch rerun is still required before treating the corrected end-to-end ratio as a controlled backend speedup.

## Resume procedure

1. Investigate the remaining reduced-precision tracking/map decline with per-layer FP16/INT8 parity probes before changing the accepted FP32 graph.
2. If exact INT8 accuracy parity is required, compare larger scene-stratified training calibration against the current separate 152-frame agent calibrations while preserving the current lineage.
3. Rerun the PyTorch open-loop baseline with the same eight-worker/load protocol before treating end-to-end speedup as a controlled backend comparison; synchronized forward is already comparable.
4. For a formal CARLA closed-loop score, wrap the two-stage cooperative backend as a V2Xverse leaderboard agent, execute the existing route/scenario manager, and validate the cooperative sensor/extrinsic contract; do not reinterpret the current custom Town03 run as a leaderboard score.
5. Update this document and push one commit after each completed major round.

---

## Iteration 013 - 2026-08-12T22:24:00-07:00

- CARLA asset inventory was cross-checked with the UniAD handoff: 233 training routes are present across Town01/Town02/Town03/Town04/Town05/Town06/Town07/Town10HD, and the complete Town01-Town06 scenario files contain 393,070 event configurations. The current UniV2X adapter is a single-route custom sensor/controller diagnostic, not an official ScenarioRunner/leaderboard evaluation.
- A complete PyTorch/FP32/FP16/INT8 sweep over all 233 routes for both models would require 1,864 route processes and a route scheduler plus official scoring integration. Existing results cover the retained Town03 diagnostic/replay paths only; no claim of full CARLA coverage is made.

---

## Iteration 013 - 2026-08-12T21:08:00-07:00

- Replaced the old single model-point steering protocol with dense GlobalRoutePlanner route conditioning; model trajectory displacement controls speed and both engines remain fixed-shape sequential stages.
- Completed UniV2X INT8 Town03 route: `3526` frames, `99.5845%` progress, destination reached at `4.687 m`, zero collisions, 28 lane invasions, zero temporal-state recoveries, infrastructure forward `110.793/110.035/118.688 ms`, ego `83.608/82.934/90.360 ms`, combined forward `200.916/199.500/216.185 ms`, service E2E `354.481/354.120/378.505 ms` (mean/p50/p99).
- The prior 1200-frame blocked result is historical: it represented 60 s for an approximately 1181.7 m route and did not follow the dense route. The corrected run still bypasses the official V2Xverse scorer, so no Driving Score is claimed.

## Iteration 012 - 2026-08-12T19:05:13-07:00

- Stopped the attempted TensorRT pip download and confirmed no downloader remained. Reused `/home/lixingfeng/uniad-trt/TensorRT-10.9_x86_cu118`; `import tensorrt` reports `10.9.0.34`, plugin dependencies resolve to this package plus Conda libraries, and FP32 dual-engine deserialization/inference passed.
- Added the original occupancy collision and predicted-drivable planning corrections to both PyTorch and TensorRT CARLA services. A 12-case comparison with `PlanningHeadSingleMode.drivable_optimization` has maximum absolute delta `0`.
- Isolated and fixed the CARLA neutral-gear defect with explicit forward gear, then completed corrected PyTorch/FP32/FP16/INT8 Town03 diagnostics using the accepted semantic-parity engines.
- Preserved the expected closed-loop forward trend: FP32 `301.013 ms` > FP16 `237.689 ms` > INT8 `206.776 ms`. All backends remained finite with no runtime fallback.
- Cooperative association still matched zero roadside tracks for every backend, and all routes terminate blocked near `1.67-1.76%`; the shared V2X-SPD/CARLA geometry-domain and host-association mismatch remains unresolved.

---

## Iteration 011 - 2026-08-12T17:40:26-07:00

- Audited the borrowed V2Xverse CARLA assets: the tree contains 42 catalogue routes and 4829 scenario trigger events, plus 233 training-split, 105 evaluation, and 66 additional route definitions.
- Corrected the earlier asset statement: the blocker is that the custom Town03 runner executes one route with zero scenario events and bypasses the official route manager/scorer, not that every route/scenario file is absent.
- Recorded that both UniV2X CARLA backends use raw `outs_planning`; together with zero accepted cooperative tracks and the V2X-SPD-to-CARLA geometry/domain gap, this explains why the PyTorch baseline and all TensorRT precisions share the early blocked failure.

---

## Iteration 010 - 2026-08-12T15:42:57-07:00

- Audited explicit-QDQ coverage for both agents: all 159/165 Conv and 47/52 Gemm weights are INT8 for infrastructure/ego, while all 675/712 MatMul nodes remain excluded; the deployment is neither Conv-only nor literal every-operator INT8.
- Added car-only metrics required for the official comparison: PyTorch/TensorRT FP32 car AMOTA is `0.236400/0.236203`, consistent with the published `0.239`; aggregate three-class AMOTA remains separately reported.
- Confirmed point/box collision for all 675-frame runs: `0.0741/0.4889%`, `0.0593/0.3704%`, `0.0444/0.3704%`, and `0.0296/0.3407%` for PyTorch, FP32, FP16, and INT8.
- Corrected the Town03 protocol with independent ego/roadside intrinsics, dense route progress, unique collision groups, and blocked termination, then retained PyTorch plus all three TensorRT latency baselines.
- Proved that all four backends produced active roadside tracks in 8/53 calls but host association matched zero; the common failure is attributed to V2X-SPD/CARLA geometry and association-contract mismatch, not INT8 alone.

---

## Iteration 009 - 2026-08-11T08:10:51-07:00

- Rebuilt corrected FP16 and separately calibrated INT8 infrastructure/ego engines from the accepted raw-map/explicit-position graph.
- Completed strict 100-frame INT8 finite smoke and full 675/675-frame all-task FP16/INT8 evaluations with zero timestamp fallback and one retained context per engine.
- Confirmed corrected forward latency `316.454 > 255.068 > 227.410 ms` for FP32, FP16, and INT8, while preserving the full detection/tracking/map/occupancy/planning metric bundle.
- Added persistent TensorRT and original-checkpoint PyTorch CARLA backends, then completed four 1200-frame Town03 model-controlled runs on identical sensors/controller inputs.
- Recorded the live closed-loop forward trend `584.822 > 303.837 > 238.823 > 213.169 ms` for PyTorch, FP32, FP16, and INT8; all routes remain diagnostic because vehicles stall after early contact.
- Retained source-only summaries under `UniV2X/evidence/corrected_precision_full675` and `UniV2X/evidence/carla_closed_loop_town03_full1200`; checkpoints, ONNX, engines, calibration tensors, raw frames, and logs remain excluded.

---

## Iteration 008 - 2026-08-10T20:58:47-07:00

- Re-exported both agents with raw map candidates and data-dependent host map postprocessing, eliminating traced `lane_pred` control-flow freezing.
- Promoted the `[1, 40000, 256]` static map-position encoding to a cached explicit TensorRT input, then built fresh fixed `960/64` FP32 engines with TF32 disabled.
- Completed all 675 validation frames with eight DataLoader workers and zero infrastructure/ego runtime fallback.
- Restored FP32 semantic parity: mAP `0.051763` vs `0.051554`, NDS `0.051958` vs `0.051359`, AMOTA `0.112422` vs `0.109535`, and planning mean L2 `2.8565` vs `2.8641` for TensorRT vs PyTorch.
- Recorded corrected forward latency `316.454/315.835/337.023 ms` and end-to-end latency `826.203/456.331/3413.873 ms` for mean/p50/p99.
- Superseded the old reduced-precision accuracy results; corrected-graph FP16 and INT8 require fresh builds and complete reruns before acceptance.

---

## Iteration 007 - 2026-08-09T22:31:24-07:00

- Expanded the full-validation comparison to include map IoUs, occupancy PQ/SQ/RQ, ten-step planning averages, and point/box collision rates for PyTorch, FP32, FP16, and INT8.
- Recorded first-frame evidence that FP32 planning/occupancy can be close while detection and map heads already diverge, then propagate through temporal cooperative inference.
- Confirmed TF32 is disabled and classified the FP32 accuracy gap as an export/plugin/runtime/reconstruction fidelity defect, not a calibration effect.
- Raised the default Python DataLoader worker count from four to eight in both PyTorch and TensorRT evaluators; retained results remain explicitly labeled as four-worker measurements.

---

## Iteration 006 - 2026-08-09T21:29:53-07:00

- Completed the fixed-960/64 TensorRT FP32 evaluation over all 675 validation frames with one retained context per engine.
- Verified exactly 675 diagnostic records, zero non-finite monitored tensors, and infrastructure/ego timestamp fallback counts `0/0`.
- Added official detection, tracking, map, occupancy, planning, and mean/p50/p99 latency summaries to the source-only evidence tree.
- Confirmed the same-runtime forward trend `FP32 315.491 ms > FP16 235.290 ms > INT8 219.240 ms`.
- Localized the dominant accuracy blocker to the pre-quantization export/runtime contract because TensorRT FP32 already differs materially from the PyTorch baseline.

---

## Iteration 003 - 2026-08-09T19:22:25-07:00

- Completed strict INT8 fixed-960/64 evaluation over all 675 validation frames: 675/675 diagnostic records were finite, with infrastructure/ego timestamp fallback counts `0/0`.
- Recorded all-task INT8 metrics and mean/p50/p99 forward plus end-to-end latency in the source-only evidence tree.
- Started the existing CARLA 0.9.10.1 server on GPU 7, captured one synchronized 20-frame Town03 sequence, and replayed FP32, FP16, and INT8 engines on identical frames.
- Confirmed the CARLA forward trend `FP32 359.287 ms > FP16 267.772 ms > INT8 247.578 ms`; all three runs were finite with zero runtime fallback.
- Fixed CARLA cleanup ordering so attached sensors are destroyed before their parent ego vehicle, avoiding a duplicate-destroy C++ abort after a successful capture.
- Stopped the managed CARLA server after replay; no additional CARLA files were downloaded.

---

## Iteration 002 - 2026-08-09T18:56:27-07:00

- Completed both stable fixed-shape INT8 engines under TensorRT `10.9.0.34`.
- Infrastructure engine: 300,419,516 bytes, build `380.893 s`, SHA-256 `6f02d6ef5ed2bfc8f49d0466493b9e642aae9795227c685963894f419bb26e8c`.
- Ego engine: 290,278,316 bytes, build `403.698 s`, SHA-256 `eda90ed3aad905b0bc4fe57731878001ae8a70a91a3c177761b4b146313588ee`.
- Passed a strict 50-frame cross-scene state test: all 50 diagnostic records were finite, frame 42 reset both agents, and timestamp fallback counts remained `0/0`.
- Cross-scene smoke latency after excluding 20 warmup frames: infrastructure `119.688/119.574/125.114 ms`, ego `88.473/88.459/92.707 ms`, combined forward `219.683/219.464/228.453 ms` for mean/p50/p99.
- End-to-end smoke latency was `2672.331/1236.327/8634.702 ms`; the gap remains dominated by CPU DataLoader stalls, not engine compute.
- Started the strict INT8 full 675-frame all-task evaluation on GPU 7.

---

## Iteration 001 - 2026-08-09T18:33:26-07:00

- Created the publication branch and source-only repository layout.
- Imported two-agent deployment tools, project/model code, custom TensorRT plugin source, CARLA adapters, and small audit reports.
- Recorded separate calibration, timestamp normalization, finite inverse-sigmoid rewrites, fixed 960/64 padding semantics, and the full FP16/PyTorch comparison.
- Added a resumable INT8 build script with an environment/toolchain guard that rejects Python, NVCC, or G++ outside the `modelopt` Conda prefix.
- Excluded weights, ONNX graphs, engines, datasets, calibration tensors, timing caches, compiled libraries, and large generated outputs.
